import React from 'react';

import { Routes, Route } from 'react-router-dom';


const AppRoutes = () => (
  <Routes>
  
    {/* Add more routes like /dashboard */}
  </Routes>
);

export default AppRoutes;

